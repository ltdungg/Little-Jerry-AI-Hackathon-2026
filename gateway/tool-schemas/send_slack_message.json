{
  "name": "send_slack_message",
  "description": "Send a slack message",
  "inputSchema": {
    "type": "object",
    "properties": {
      "tenant_id": {"type": "string"},
      "confirmation_token": {"type": "string"},
      "idempotency_key": {"type": "string"}
    },
    "required": ["tenant_id", "confirmation_token", "idempotency_key"]
  }
}
